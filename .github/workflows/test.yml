name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26"

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Download dependencies
        run: go mod download

      - name: Download CLI dependencies
        working-directory: cli
        run: go mod download

      - name: Download CLI integration dependencies
        working-directory: cli/integration
        run: go mod download

      - name: Verify dependencies
        run: go mod verify

      - name: Verify CLI dependencies
        working-directory: cli
        run: go mod verify

      - name: Verify CLI integration dependencies
        working-directory: cli/integration
        run: go mod verify

      - name: Run tests
        run: go test -v -race -coverprofile=coverage.txt -covermode=atomic $(go list ./... | grep -v /e2e)

      - name: Run CLI tests
        working-directory: cli
        run: go test -v ./...

      - name: Run CLI integration tests
        working-directory: cli/integration
        run: go test -tags integration -v -timeout 60s ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          file: ./coverage.txt
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Build
        run: go build -v -o lunar ./cmd

      - name: Build CLI
        working-directory: cli
        run: go build -v -o lunar-cli .

      - name: Run vet
        run: go vet ./...

      - name: Run vet on CLI
        working-directory: cli
        run: go vet ./...

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest

  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26"

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Download dependencies
        run: go mod download

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      - name: Run e2e tests
        run: make test-e2e
